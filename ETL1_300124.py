import pandas as pddfres = pd.read_excel("residentdoctors.xlsx")print(dfres.info())# transforming data, first renaming a col and then extracting specific info from itdfres["Lower_Age"] = dfres["AGEDIST"].str.extract('(\d+)-')# changing the string to intergerdfres["Lower_Age"] = dfres["Lower_Age"].astype(int)print(dfres.info())"""Working with dates"""dfdates = pd.read_csv("time_series_data.csv", index_col=0)print(dfdates.info())dfdates['Date'] = pd.to_datetime(dfdates['Date'])print(dfdates.info())dfdates['Year'] = dfdates['Date'].dt.yeardfpat = pd.read_csv("patient_data_dates.csv", index_col=0)print(dfpat.info())dfpat.drop(index = 26, inplace = True)dfpat['Date'] = pd.to_datetime(dfpat['Date'])print(dfpat.info())ave_cal = dfpat["Calories"].mean()dfpat["Calories"].fillna(ave_cal, inplace = True)"""best practices"""# to remove nansdfpat = dfpat.reset_index(drop = True)# To replace a value dfpat['Duration'] = dfpat['Duration'].replace([450], 50)                          # pd.set_option('display.max_rows), None                          )